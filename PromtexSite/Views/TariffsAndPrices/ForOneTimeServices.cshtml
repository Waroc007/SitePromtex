@model List<TablePrice>
@{
    ViewData["Title"] = "Цены на разовые услуги";
}
<div class="container">
    <h3>Цены на разовые услуги</h3>
    <div class="row">
        <div class="col-12 col-lg-8" style="text-align:justify;">
            <div class="js-tab-content-1">
                <div class="faq_item js-faq-item">
                    <span class="click_to_show">Ведение учета</span>
                    <div class="hidden_content">
                        <table class="table table-bordered table-hover">
                            <thead><tr><th>Наименование</th><th>Ед. измерения</th><th>Цена, руб.</th></tr></thead>
                            <tbody>
                                @foreach (var t in Model)
                                {
                                    @if (t.Type == TableType.RecordKeeping)
                                    {
                                        <tr>
                                            <td>@t.Name</td>
                                            <td>@t.Unit</td>
                                            <td>@t.Price.ToString("#")</td>
                                        </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
                <div class="faq_item js-faq-item">
                    <span class="click_to_show">Кадровый учет</span><div class="hidden_content">
                        <table class="table table-bordered table-hover">
                            <thead><tr><th>Наименование</th><th>Ед. измерения</th><th>Цена, руб.</th></tr></thead>
                            <tbody>
                                @foreach (var t in Model)
                                {
                                    @if (t.Type == TableType.PersonnelAccounting)
                                    {
                                        <tr>
                                            <td>@t.Name</td>
                                            <td>@t.Unit</td>
                                            <td>@t.Price.ToString("#")</td>
                                        </tr>
                                    }
                                }

                            </tbody>
                        </table>
                    </div>
                </div><div class="faq_item js-faq-item">
                    <span class="click_to_show">Расчет заработной платы</span><div class="hidden_content">
                        <table class="table table-bordered table-hover">
                            <thead><tr><th>Наименование</th><th>Ед. измерения</th><th>Цена, руб.</th></tr></thead>
                            <tbody>
                                @foreach (var t in Model)
                                {
                                    @if (t.Type == TableType.PayrollPreparation)
                                    {
                                        <tr>
                                            <td>@t.Name</td>
                                            <td>@t.Unit</td>
                                            <td>@t.Price.ToString("#")</td>
                                        </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
                <div class="faq_item js-faq-item">
                    <span class="click_to_show">Составление и сдача отчетности</span><div class="hidden_content">
                        <table class="table table-bordered table-hover">
                            <thead><tr><th>Наименование</th><th>Ед. измерения</th><th>Цена, руб.</th></tr></thead>
                            <tbody>
                                @foreach (var t in Model)
                                {
                                    @if (t.Type == TableType.ReportingAndSubmission)
                                    {
                                        <tr>
                                            <td>@t.Name</td>
                                            <td>@t.Unit</td>
                                            <td>@t.Price.ToString("#")</td>
                                        </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
                <div class="faq_item js-faq-item">
                    <span class="click_to_show">Услуги хранения</span><div class="hidden_content">
                        <table class="table table-bordered table-hover">
                            <thead><tr><th>Наименование</th><th>Ед. измерения</th><th>Цена, руб.</th></tr></thead>
                            <tbody>
                                @foreach (var t in Model)
                                {
                                    @if (t.Type == TableType.StorageServices)
                                    {
                                        <tr>
                                            <td>@t.Name</td>
                                            <td>@t.Unit</td>
                                            <td>@t.Price.ToString("#")</td>
                                        </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
                <div class="faq_item js-faq-item">
                    <span class="click_to_show">Консультации по вопросам ведения бухгалтерского и налогового учета</span><div class="hidden_content">
                        <table class="table table-bordered table-hover">
                            <thead><tr><th>Наименование</th><th>Ед. измерения</th><th>Цена, руб.</th></tr></thead>
                            <tbody>
                                @foreach (var t in Model)
                                {
                                    @if (t.Type == TableType.AccountingAdvice)
                                    {
                                        <tr>
                                            <td>@t.Name</td>
                                            <td>@t.Unit</td>
                                            <td>@t.Price.ToString("#")</td>
                                        </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
                <div class="faq_item js-faq-item">
                    <span class="click_to_show">Курьерская служба/услуги секретаря</span><div class="hidden_content">
                        <table class="table table-bordered table-hover">
                            <thead><tr><th>Наименование</th><th>Ед. измерения</th><th>Цена, руб.</th></tr></thead>
                            <tbody>
                                @foreach (var t in Model)
                                {
                                    @if (t.Type == TableType.SecretaryServices)
                                    {
                                        <tr>
                                            <td>@t.Name</td>
                                            <td>@t.Unit</td>
                                            <td>@t.Price.ToString("#")</td>
                                        </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
                <div class="faq_item js-faq-item">
                    <span class="click_to_show">Юридические услуги</span><div class="hidden_content">
                        <table class="table table-bordered table-hover">
                            <thead><tr><th>Наименование</th><th>Ед. измерения</th><th>Цена, руб.</th></tr></thead>
                            <tbody>
                                @foreach (var t in Model)
                                {
                                    @if (t.Type == TableType.LegalServices)
                                    {
                                        <tr>
                                            <td>@t.Name</td>
                                            <td>@t.Unit</td>
                                            <td>@t.Price.ToString("#")</td>
                                        </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
        <div class="col">
            <div class="row" style="border: 1px solid rgb(250, 208, 72);background-image:url(../img/banner_buh1.png);background-repeat: no-repeat;background-position: right bottom; width:300px;height:430px; position: relative; left: 10px;">
                <p style="margin:5px;">Хотите получить консультацию?<br /><strong>Оставьте заявку и мы вам перезвоним</strong></p>
                <div class="d-block mx-auto" style="text-align:center;position: absolute;bottom: 10px;right: 10px;">
                    <button class="btn btn-danger mb-1 btn-lg" data-toggle="modal" data-target="#Connect">Оставить заявку</button>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    <script>
        $(".click_to_show").click(
            function () {
                $(this).toggleClass('black');
                $(this).siblings(".hidden_content").slideToggle("slow");
            }
        );
    </script>
}